import React from "react";
import ReactDOM from "react-dom/client";
import FootballAgent from "./FootballAgent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FootballAgent />
  </React.StrictMode>
);
